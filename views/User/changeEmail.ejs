<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Exclusive Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->




</head>
<body>
    


<%- include("../partials/header.ejs") %>


<section class="content-main mt-80 mb-80">
 <div class="container-fluid login-container">
        <div class="row">
         
            <div class="col-lg-6 d-flex justify-content-center align-items-center
   
    <div class="card mx-auto card-login" style="max-width: 400px;">
        
        <div class="card-body">
            <h4 class="card-title mb-4 text-center">Change Email</h4>
            <form method="post" action="/user/changeEmail" onsubmit="return validateForm()">
                <div class="mb-3 position-relative">
                    <input class="form-control"  placeholder="Enter new Email" type="email" required style="border: 1px solid #ced4da;" name="newEmail">
                  
                </div>
                
                <div class="mb-4">
                    <button type="submit" class="btn btn-primary w-100">Change Email</button>
                </div>
                      <% if (locals.message && message.length > 0) { %>
                    <div id="alertMessage" class="alert alert-danger text-center">
                        <%= message %>
                    </div>
                <% } %>
            </form>
            <p class="text-center mt-4">Don't have an account? <a href="/user/profile">go back to profile</a></p>
        </div>
    </div>
    </div>

    </div>

    
 </section>

 
 </body>
 </html>
 