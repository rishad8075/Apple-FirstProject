<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Email | Exclusive</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #f5f7fa;
            font-family: "Inter", sans-serif;
        }

        .change-email-card {
            background: #ffffff;
            border-radius: 18px;
            padding: 35px;
            box-shadow: 0px 10px 35px rgba(0, 0, 0, 0.08);
            transition: 0.3s ease;
        }

        .change-email-card:hover {
            transform: translateY(-3px);
        }

        .page-title {
            font-weight: 700;
            color: #333;
        }

        .form-control {
            padding: 12px;
            border-radius: 12px;
        }

        .btn-primary {
            padding: 12px;
            border-radius: 12px;
            font-weight: 600;
        }

        a {
            text-decoration: none;
            font-weight: 600;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

<%- include("../partials/header.ejs") %>

<section class="content-main mt-5 mb-5">
    <div class="container d-flex justify-content-center">
        <div class="change-email-card" style="max-width: 420px; width:100%;">

            <h3 class="text-center page-title mb-4">Change Email</h3>

            <form method="post" action="/user/changeEmail" onsubmit="return validateForm()">

                <div class="mb-3">
                    <label class="form-label">New Email</label>
                    <input 
                        class="form-control"
                        type="email"
                        placeholder="Enter your new email"
                        name="newEmail"
                        required
                    >
                </div>

                <% if (locals.message && message.length > 0) { %>
                    <div id="alertMessage" class="alert alert-danger text-center mt-3">
                        <%= message %>
                    </div>
                <% } %>

                <div class="mt-4">
                    <button type="submit" class="btn btn-primary w-100">Update Email</button>
                </div>
            </form>

            <p class="text-center mt-4">
                Go back to <a href="/user/profile">Profile</a>
            </p>

        </div>
    </div>
</section>

</body>
</html>
