<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us | YourBrand</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        :root {
            --primary-accent: #0071e3; /* Apple Blue */
            --bg-soft: #f5f5f7;
            --text-dark: #1d1d1f;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--text-dark);
            background-color: #fff;
        }

        /* Hero Section */
        .contact-hero {
            position: relative;
            height: 400px;
            background: url('https://images.unsplash.com/photo-1497215728101-856f4ea42174?auto=format&fit=crop&q=80&w=2070') center/cover no-repeat;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.4);
            backdrop-filter: blur(2px);
        }

        .hero-content {
            position: relative;
            color: #fff;
            z-index: 1;
        }

        .hero-content h1 {
            font-size: 3rem;
            font-weight: 700;
        }

        /* Form Container */
        .contact-wrapper {
            max-width: 900px;
            margin: -60px auto 60px;
            background: #fff;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        .contact-info-bar {
            background: #fafafa;
            padding: 30px;
            border-bottom: 1px solid #eee;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .info-item i {
            font-size: 1.5rem;
            color: var(--primary-accent);
        }

        /* Form Styling */
        .contact-form-section {
            padding: 50px;
        }

        .form-label {
            font-size: 0.85rem;
            font-weight: 600;
            color: #6e6e73;
        }

        .form-control {
            border-radius: 10px;
            padding: 12px 15px;
            border: 1px solid #d2d2d7;
            background-color: #fff;
            transition: all 0.2s;
        }

        .form-control:focus {
            border-color: var(--primary-accent);
            box-shadow: 0 0 0 4px rgba(0, 113, 227, 0.1);
        }

        .btn-submit {
            background-color: var(--text-dark);
            color: #fff;
            padding: 12px 40px;
            border-radius: 30px;
            border: none;
            font-weight: 600;
            transition: 0.3s;
        }

        .btn-submit:hover {
            background-color: #000;
            transform: translateY(-2px);
        }

        /* Features Section (from Figma) */
        .features-strip {
            background-color: var(--bg-soft);
            padding: 60px 0;
        }

        .feature-box {
            text-align: center;
        }

        .feature-box h5 {
            font-weight: 700;
            margin-bottom: 5px;
        }

        .feature-box p {
            font-size: 0.9rem;
            color: #6e6e73;
        }

        @media (max-width: 768px) {
            .contact-form-section { padding: 30px 20px; }
            .hero-content h1 { font-size: 2.2rem; }
            .contact-wrapper { margin-top: -30px; border-radius: 0; }
        }
    </style>
</head>
<body>

    <header><%- include("../partials/header.ejs") %></header>

    <section class="contact-hero">
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <h1>Get in Touch With Us</h1>
            <p class="opacity-75">Have questions? We're here to help you instantly.</p>
        </div>
    </section>

    <div class="container">
        <div class="contact-wrapper">
            <div class="contact-info-bar">
                <div class="row text-center g-4">
                    <div class="col-md-6 info-item justify-content-center">
                        <i class="bi bi-geo-alt"></i>
                        <div class="text-start">
                            <span class="d-block fw-bold">Address</span>
                            <small class="text-muted">Kannur, Edayannur, India</small>
                        </div>
                    </div>
                    <div class="col-md-6 info-item justify-content-center">
                        <i class="bi bi-telephone"></i>
                        <div class="text-start">
                            <span class="d-block fw-bold">Phone</span>
                            <small class="text-muted">+91 8075147355</small>
                        </div>
                    </div>
                </div>
            </div>

            <div class="contact-form-section">
                <form id="contactForm" action="/contact/send" method="POST">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <label class="form-label">Your Name</label>
                            <input type="text" name="name" class="form-control" placeholder="ABC" required>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Email Address</label>
                            <input type="email" name="email" class="form-control" placeholder="abc@gmail.com" required>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Subject</label>
                            <input type="text" name="subject" class="form-control" placeholder="This is optional">
                        </div>
                        <div class="col-12">
                            <label class="form-label">Message</label>
                            <textarea name="message" class="form-control" rows="5" placeholder="Hi, I'd like to ask about..." required></textarea>
                        </div>
                        <div class="col-12 text-center">
                            <button type="submit" class="btn-submit">Submit Message</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <section class="features-strip">
        <div class="container">
            <div class="row g-4">
                <div class="col-md-4 feature-box">
                    <h5>Free Delivery</h5>
                    <p>For all orders over $50</p>
                </div>
                <div class="col-md-4 feature-box">
                    <h5>90 Days Return</h5>
                    <p>If goods have problems</p>
                </div>
                <div class="col-md-4 feature-box">
                    <h5>Secure Payment</h5>
                    <p>100% secure payment</p>
                </div>
            </div>
        </div>
    </section>

    <footer><%- include("../partials/footer.ejs") %></footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>