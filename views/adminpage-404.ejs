<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 | System Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Plus+Jakarta+Sans:wght@400;600;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        :root {
            --admin-primary: #4f46e5; /* Indigo Admin Theme */
            --admin-dark: #0f172a;
            --admin-bg: #f8fafc;
        }
        body { 
            background-color: var(--admin-bg); 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            height: 100vh; display: flex; align-items: center; justify-content: center;
        }
        .admin-card {
            background: #fff; border: 1px solid #e2e8f0; border-radius: 20px;
            padding: 3rem; max-width: 550px; width: 90%;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .error-code {
            font-family: 'JetBrains Mono', monospace;
            font-size: 6rem; font-weight: 800; color: var(--admin-dark);
            line-height: 1; margin-bottom: 1rem;
        }
        .badge-status {
            background: #fee2e2; color: #ef4444;
            padding: 5px 12px; border-radius: 6px; font-weight: 700; font-size: 0.8rem;
        }
        .btn-admin-home {
            background: var(--admin-dark); color: white;
            padding: 12px 24px; border-radius: 10px; font-weight: 600;
            text-decoration: none; transition: 0.2s; display: inline-block;
        }
        .btn-admin-home:hover { background: var(--admin-primary); color: white; transform: translateY(-2px); }
        .path-display {
            background: #f1f5f9; padding: 8px 12px; border-radius: 8px;
            font-family: 'JetBrains Mono', monospace; font-size: 0.85rem; color: #64748b;
        }
    </style>
</head>
<body>
    <div class="admin-card">
        <div class="mb-4 text-muted"><i class="fas fa-terminal fa-3x"></i></div>
        <div class="error-code">404</div>
        <div class="mb-3"><span class="badge-status"><%= errorMessage %></span></div>
        <h2 class="fw-bold mb-3">Resource Missing</h2>
        <p class="text-secondary mb-4">The administrative resource you requested could not be located on the server.</p>
        
        <div class="path-display mb-4">
            <i class="fas fa-link me-2"></i> admin/system/internal-resource
        </div>

        <div class="d-flex flex-column gap-2">
            <a href="/admin/dashboard" class="btn-admin-home">
                <i class="fas fa-chart-line me-2"></i> Back to Dashboard
            </a>
            <a href="javascript:location.reload()" class="btn btn-link text-decoration-none text-muted small">
                <i class="fas fa-sync me-1"></i> Retry Request
            </a>
        </div>
    </div>
</body>
</html>